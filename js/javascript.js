window.onload = function() {
    const skillDamage = 300; // 변경할 값을 변수로 지정합니다.
    const skillDamageElement = document.getElementById('skill-damage'); // 값을 변경할 요소를 식별합니다.
    skillDamageElement.innerText = `[${skillDamage}]`; // 요소 내의 값을 변수로 변경합니다.
  }

// 10.1 어둠의 도가니, 아베루스

class AberrusBoss {
    name = null
    summary = null
    Boss1() {
        
    } 
}

